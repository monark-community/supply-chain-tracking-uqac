####################################
# 🔗 Blockchain Configuration
####################################
# RPC endpoint for interacting with the blockchain
RPC_URL=https://your-rpc-url-here
# Smart contract deployed address
CONTRACT_ADDRESS=0xYourContractAddressHere
# NEVER commit your private key; this is only an example placeholder
PRIVATE_KEY=your-private-key-here
# Transaction hash example (optional)
TX_HASH=your-tx-hash-here
# Tenderly project configuration (optional)
TENDERLY_USERNAME=your-tenderly-username
TENDERLY_PROJECT=your-tenderly-project
TENDERLY_AUTOMATIC_VERIFICATION=true


####################################
# 🧠 Backend (Express + PostgreSQL)
####################################
# PostgreSQL connection settings
DB_HOST=db
DB_PORT=5432
DB_NAME=coffee_db
DB_USER=postgres
DB_PASS=root

# Optional full connection string for ORMs
DATABASE_URL=postgres://postgres:root@db:5432/coffee_db


####################################
# 💻 Frontend (React + Auth0)
####################################
# Auth0 configuration for authentication
AUTH0_SECRET=your-auth0-secret
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=https://your-auth0-domain.us.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret

# Public API base URL used by the Next.js frontend
# Local npm dev: set to http://localhost:5000
# Docker-compose: set to http://backend:5000
NEXT_PUBLIC_API_URL=http://localhost:5000


####################################
# 🐳 Docker Configuration
####################################
# PostgreSQL ports (internal and external)
DB_PORT_IN=5432
DB_PORT_OUT=5432

# PGAdmin configuration
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT_IN=80
PGADMIN_PORT_OUT=8080

####################################
# ℹ️ Notes
####################################
# Local development:
# - Create .env.local at repo root and copy relevant values from this file.
# - Ensure NEXT_PUBLIC_API_URL=http://localhost:5000 for frontend.
# Docker-compose:
# - Copy this file to .env at repo root and adjust values.
# - The backend automatically loads .env in Docker; frontend reads NEXT_PUBLIC_API_URL.
