####################################
# 🔗 Blockchain Configuration
####################################
RPC_URL=https://your-rpc-url-here
CONTRACT_ADDRESS=0xYourContractAddressHere
PRIVATE_KEY=your-private-key-here
TX_HASH=your-tx-hash-here
TENDERLY_USERNAME=your-tenderly-username
TENDERLY_PROJECT=your-tenderly-project
TENDERLY_AUTOMATIC_VERIFICATION=true

####################################
# 🧠 Backend (Express + PostgreSQL)
####################################
DB_HOST=db
DB_PORT=5432
DB_NAME=coffee_db
DB_USER=postgres
DB_PASS=root

# Full connection string for ORMs / Prisma / Sequelize
DATABASE_URL=postgres://postgres:root@db:5432/coffee_db

# Backend URL (local: localhost, prod: Render)
BASE_URL=http://localhost:5000
PORT=5000
NODE_ENV=development

####################################
# 💻 Frontend (React + Auth0)
####################################
AUTH0_SECRET=your-auth0-secret
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=https://your-auth0-domain.us.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret

# Public API base URL used by Next.js frontend
# Local npm dev: http://localhost:5000
# Docker / prod: set to deployed backend URL
NEXT_PUBLIC_API_URL=http://localhost:5000

####################################
# 🐳 Docker Configuration
####################################
DB_PORT_IN=5432
DB_PORT_OUT=5432

PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT_IN=80
PGADMIN_PORT_OUT=8080

####################################
# ℹ️ Notes
####################################
# Local development:
# - Create .env.local and copy values from this file.
# - NEXT_PUBLIC_API_URL=http://localhost:5000 for frontend.
# - BASE_URL=http://localhost:5000 for backend Swagger & logs.

# Docker-compose:
# - Copy this file to .env at root and adjust values.
# - Backend automatically loads .env; frontend reads NEXT_PUBLIC_API_URL.

# Production (Render):
# - Set BASE_URL=https://your-backend-render-url
# - Set NEXT_PUBLIC_API_URL=https://your-backend-render-url
# - NODE_ENV=production
